kind = "web"
previewPath = "/"
title = "PulseFlow Dashboard"
version = "1.0.0"
id = "artifacts/pulseflow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19692

[services.development]
run = "pnpm --filter @workspace/pulseflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pulseflow", "run", "build" ]
publicDir = "artifacts/pulseflow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19692"
BASE_PATH = "/"
